#!/bin/bash

# Vinted AI Assistant - macOS Installer
# Double-click this file to install or start the application

# Change to the script directory
cd "$(dirname "$0")"

# Make sure the install script is executable
chmod +x scripts/install.sh

# Run the installation script
./scripts/install.sh
